/**
 * VibeGuard — Version (single source of truth)
 *
 * Keep this value in sync with package.json. Used by the CLI banner,
 * `--version`, and every HTTP User-Agent string so they always agree.
 */

export const VERSION = "1.1.0";
